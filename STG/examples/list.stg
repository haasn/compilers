nil  = \     n c -> n ;
cons = \x xs n c -> c x xs ;

map = \f l -> l l (\x xs -> cons (f x) (map f xs)) ;
sum = \l -> l zero (\x xs -> add x (sum xs)) ;

onetwothree = cons one (cons two (cons three nil))
